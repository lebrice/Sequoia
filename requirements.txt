pytorch-lightning>=1.1.2
# Fork of gym with more flexible utility functions.
gym[atari] @ git+https://www.github.com/lebrice/gym@easier_custom_spaces
# nngeometry module used in the EWC method
nngeometry @ git+https://github.com/oleksost/nngeometry.git
simple_parsing>=0.0.12.post7
matplotlib
torch>=1.7
torchvision>=0.8.2
scikit-learn
tqdm
continuum>=1.0.5
# Only required for the current demo:
wandb>=0.10.17
plotly
pandas
# Only for python < 3.8
singledispatchmethod;python_version<'3.8'
pytorch-lightning-bolts
# Requirements for running tests:
pytest-timeout
pytest-xdist
pytest-xvfb # Prevents the gym popups from displaying during tests.
# Required for the RL methods
# TODO: Move this to an 'extras' in setup.py?
stable-baselines3
pyvirtualdisplay
# Required for the synbols dataset to work. 
h5py
